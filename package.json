{
  "name": "zeplin-emotion",
  "version": "0.1.0",
  "description": "Extension to translate styles to emotion.",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec",
    "publish": "zem publish",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "zeplin": {
    "displayName": "zeplin-emotion",
    "platforms": [
      "web",
      "osx"
    ]
  },
  "dependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.6.2",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "zem": "^0.4.3"
  },
  "devDependencies": {
    "typescript": "^3.5.3"
  }
}
